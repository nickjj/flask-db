## Installation

```sh
# Ensure the FLASK_APP is set.
export FLASK_APP=tests.example_app.example.app

# Install the dependencies.
pip install -r requirements.txt

# Check out the DB commands.
flask db
```
